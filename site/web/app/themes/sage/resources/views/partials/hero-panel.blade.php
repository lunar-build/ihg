<section class="hero-panel">
    <h1 class="hero_heading">{{ $hero_heading }}</h1>
    <div class="hero-text">
        <h2 class="hero-subheading">{{ $hero_subheading }}</h2>
        <hr class="hero-rule">
        <p class="hero-text">{{ $hero_text }}</p> 
    </div>
</section>

{{--IMAGE  --}}
{{-- {!! App::create_responsive_image($hero_image) !!} --}}